{
	"name": "@pylon-sync/provider-github",
	"version": "0.0.1",
	"description": "GitHub provider for pylon-sync using the REST API",
	"type": "module",
	"license": "MIT",
	"author": "Martin Garcia",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/magarcia/pylon-sync.git",
		"directory": "packages/provider-github"
	},
	"main": "src/index.ts",
	"types": "src/index.ts",
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit",
		"build": "tsc --noEmit"
	},
	"dependencies": {
		"@pylon-sync/core": "workspace:*",
		"fflate": "^0.8.2"
	},
	"devDependencies": {
		"typescript": "^5.7.0",
		"vitest": "^3.0.0"
	}
}

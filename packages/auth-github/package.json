{
	"name": "@pylon-sync/auth-github",
	"version": "0.0.1",
	"description": "GitHub App device flow authentication for pylon-sync",
	"type": "module",
	"license": "MIT",
	"author": "Martin Garcia",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/magarcia/pylon-sync.git",
		"directory": "packages/auth-github"
	},
	"main": "src/index.ts",
	"types": "src/index.ts",
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit",
		"build": "tsc --noEmit"
	},
	"dependencies": {
		"@pylon-sync/core": "workspace:*"
	},
	"devDependencies": {
		"typescript": "^5.7.0",
		"vitest": "^3.0.0"
	}
}
